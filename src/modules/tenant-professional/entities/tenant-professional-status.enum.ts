/** Status operacional do vínculo profissional ↔ tenant (Fase 2). */
export enum TenantProfessionalStatus {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
  SUSPENDED = 'SUSPENDED',
  LEFT = 'LEFT',
}
