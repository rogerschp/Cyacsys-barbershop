/**
 * Re-export: implementação canônica em `src/common/utils/normalize-phone`.
 * Mantido para imports existentes do módulo booking.
 */
export {
  normalizePhone,
  tryNormalizePhone,
} from '../../../common/utils/normalize-phone';
